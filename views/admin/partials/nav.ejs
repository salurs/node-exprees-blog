  <!-- Navbar -->
  <nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
      </li>
    </ul>
    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
      <!-- Messages Dropdown Menu -->
      <li class="nav-item dropdown">
        <a class="nav-link" data-toggle="dropdown" href="#">
          <i class="far fa-comments"></i>
          <span class="badge bg-olive navbar-badge"><%= unRead %></span>
        </a>
        <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
          <!-- Message Start -->
          <% if(lastContacts){ %>
            <% lastContacts.forEach(function(conatct,i){ %>
                <a href="/admin/contacts/<%= conatct._id %>" class="dropdown-item">
                  <div class="media">
                    <img src="/admin/dist/img/user.png" alt="User Avatar" class="img-size-50 mr-3 img-circle">
                    <div class="media-body">
                      <h3 class="dropdown-item-title font-weight-bold"><%= conatct.name %></h3>
                      <p class="text-sm"><%= conatct.email %></p>
                      <p class="text-sm text-muted"><i class="far fa-clock mr-1"></i> <%= conatct.newDate %></p>
                    </div>
                  </div>
                </a>
                <div class="dropdown-divider my-2"></div>
              <% }); %>
            <% } %>
            <!-- Message End -->
          <div class="dropdown-divider"></div>
    
          <a href="/admin/contacts" class="dropdown-item dropdown-footer">See All Messages</a>
        </div>
      </li>
      <!-- Notifications Dropdown Menu -->
      <li class="nav-item dropdown">
        <a class="nav-link" data-toggle="dropdown" href="#">
            <img style="width: 25px;" src="/admin/dist/img/user.png" class="img-circle elevation-2" alt="User Image">
        </a>
        <div class="dropdown-menu dropdown-menu-lg dropdown-menu-light bg-dark">
          <div class="row ">
              <div class="col-12 p-3 text-center">
                <img style="width: 160px;" src="/admin/dist/img/user.png" class="img-circle elevation-2" alt="User Image">
                <div class="font-weight-bold"><%=  user.username %></div>
                <div class="">(Admin)</div>
              </div>
          </div>
          <div class="dropdown-divider"></div>
          <div class="row">
              <div class="col-12 p-3">
                  <div class="d-flex justify-content-between">
                      <div class="btn btn-outline-info">Profile</div>
                      <div>
                        <form action="/admin?_method=delete" method="post">
                          <button class="btn btn-outline-danger">Logout</button>
                        </form>
                      </div>
                  </div>
              </div>
          </div>
        </div>
      </li>
    </ul>
  </nav>
  <!-- /.navbar -->
